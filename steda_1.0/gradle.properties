# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Needed because Firebase/Google libraries depend on AndroidX artifacts.
android.useAndroidX=true
android.enableJetifier=true

# Keeps Android Studio from producing confusing configuration-cache HTML warnings while developing.
org.gradle.configuration-cache=false
